# "static" mode browses the cached snapshot in public/trades.json with no
# backend required. Any other value (or unset) uses the live API below.
NEXT_PUBLIC_DATA_MODE=api

NEXT_PUBLIC_API_URL=http://localhost:3000
